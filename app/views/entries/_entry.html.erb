<%= turbo_frame_tag dom_id(entry) do %>
  <%= render(entry.person.baby_mode? ? "entries/baby_entry" : "entries/protocol_entry", entry: entry, show_date: local_assigns[:show_date], date: local_assigns[:date]) %>
<% end %>
